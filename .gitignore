# Dependências (ignorar em todo o repo)
node_modules

# Build de produção (quem envia é o GitHub Action)
build

# Artefatos gerados localmente
.docusaurus
.cache-loader

# Arquivos de lock NÃO devem ser ignorados!
# package-lock.json  ← remova essa linha do .gitignore
# yarn.lock          ← se usar yarn, mantenha versionado

# Outras coisas
.DS_Store
.env*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
